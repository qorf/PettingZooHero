package Templates.PettingZooHero

/*Turtles are big animals that take 3 food to feed.*/
class Turtle is Animal
    
    on create
        SetName("Turtle")
        SetDescription("Turtles are big eaters! They only move one space, but they want 3 food.")
        SetImageFile("Turtle.png")
        SetHP(3)
    end
    
end
